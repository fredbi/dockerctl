// Code generated by go-swagger; DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"

	"github.com/go-swagger/dockerctl/client/image"

	"github.com/spf13/cobra"

	"github.com/go-openapi/swag"
)

// makeOperationImageImageListCmd returns a command to handle operation imageList
func makeOperationImageImageListCmd() (*cobra.Command, error) {
	cmd := &cobra.Command{
		Use:   "ImageList",
		Short: `Returns a list of images on the server. Note that it uses a different, smaller representation of an image than inspecting a single image.`,
		RunE:  runOperationImageImageList,
	}

	if err := registerOperationImageImageListParamFlags(cmd); err != nil {
		return nil, err
	}

	return cmd, nil
}

// runOperationImageImageList uses cmd flags to call endpoint api
func runOperationImageImageList(cmd *cobra.Command, args []string) error {
	appCli, err := makeClient(cmd, args)
	if err != nil {
		return err
	}
	// retrieve flag values from cmd and fill params
	params := image.NewImageListParams()
	if err, _ = retrieveOperationImageImageListAllFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationImageImageListDigestsFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationImageImageListFiltersFlag(params, "", cmd); err != nil {
		return err
	}
	if dryRun {
		logDebugf("dry-run flag specified. Skip sending request.")
		return nil
	}
	// make request and then print result
	msgStr, err := parseOperationImageImageListResult(appCli.Image.ImageList(params))
	if err != nil {
		return err
	}

	if !debug {
		fmt.Println(msgStr)
	}

	return nil
}

// registerOperationImageImageListParamFlags registers all flags needed to fill params
func registerOperationImageImageListParamFlags(cmd *cobra.Command) error {
	if err := registerOperationImageImageListAllParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationImageImageListDigestsParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationImageImageListFiltersParamFlags("", cmd); err != nil {
		return err
	}
	return nil
}

func registerOperationImageImageListAllParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagAllDescription := `Show all images. Only images from a final layer (no children) are shown by default.`

	var flagAllName string
	if cmdPrefix == "" {
		flagAllName = "all"
	} else {
		flagAllName = fmt.Sprintf("%v.all", cmdPrefix)
	}

	var flagAllDefault bool

	_ = cmd.PersistentFlags().Bool(flagAllName, flagAllDefault, flagAllDescription)

	return nil
}

func registerOperationImageImageListDigestsParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagDigestsDescription := `Show digest information as a ` + "`" + `RepoDigests` + "`" + ` field on each image.`

	var flagDigestsName string
	if cmdPrefix == "" {
		flagDigestsName = "digests"
	} else {
		flagDigestsName = fmt.Sprintf("%v.digests", cmdPrefix)
	}

	var flagDigestsDefault bool

	_ = cmd.PersistentFlags().Bool(flagDigestsName, flagDigestsDefault, flagDigestsDescription)

	return nil
}

func registerOperationImageImageListFiltersParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagFiltersDescription := `A JSON encoded value of the filters (a ` + "`" + `map[string][]string` + "`" + `) to process on the images list. Available filters:

- ` + "`" + `before` + "`" + `=(` + "`" + `<image-name>[:<tag>]` + "`" + `,  ` + "`" + `<image id>` + "`" + ` or ` + "`" + `<image@digest>` + "`" + `)
- ` + "`" + `dangling=true` + "`" + `
- ` + "`" + `label=key` + "`" + ` or ` + "`" + `label="key=value"` + "`" + ` of an image label
- ` + "`" + `reference` + "`" + `=(` + "`" + `<image-name>[:<tag>]` + "`" + `)
- ` + "`" + `since` + "`" + `=(` + "`" + `<image-name>[:<tag>]` + "`" + `,  ` + "`" + `<image id>` + "`" + ` or ` + "`" + `<image@digest>` + "`" + `)
`

	var flagFiltersName string
	if cmdPrefix == "" {
		flagFiltersName = "filters"
	} else {
		flagFiltersName = fmt.Sprintf("%v.filters", cmdPrefix)
	}

	var flagFiltersDefault string

	_ = cmd.PersistentFlags().String(flagFiltersName, flagFiltersDefault, flagFiltersDescription)

	return nil
}

func retrieveOperationImageImageListAllFlag(m *image.ImageListParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("all") {

		var flagAllName string
		if cmdPrefix == "" {
			flagAllName = "all"
		} else {
			flagAllName = fmt.Sprintf("%v.all", cmdPrefix)
		}

		flagAllValue, err := cmd.Flags().GetBool(flagAllName)
		if err != nil {
			return err, false
		}
		m.All = &flagAllValue

	}

	return nil, retAdded
}

func retrieveOperationImageImageListDigestsFlag(m *image.ImageListParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("digests") {

		var flagDigestsName string
		if cmdPrefix == "" {
			flagDigestsName = "digests"
		} else {
			flagDigestsName = fmt.Sprintf("%v.digests", cmdPrefix)
		}

		flagDigestsValue, err := cmd.Flags().GetBool(flagDigestsName)
		if err != nil {
			return err, false
		}
		m.Digests = &flagDigestsValue

	}

	return nil, retAdded
}

func retrieveOperationImageImageListFiltersFlag(m *image.ImageListParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("filters") {

		var flagFiltersName string
		if cmdPrefix == "" {
			flagFiltersName = "filters"
		} else {
			flagFiltersName = fmt.Sprintf("%v.filters", cmdPrefix)
		}

		flagFiltersValue, err := cmd.Flags().GetString(flagFiltersName)
		if err != nil {
			return err, false
		}
		m.Filters = &flagFiltersValue

	}

	return nil, retAdded
}

// parseOperationImageImageListResult parses request result and return the string content
func parseOperationImageImageListResult(resp0 *image.ImageListOK, respErr error) (string, error) {
	if respErr != nil {

		// responses
		var iResp0 any = respErr
		eresp0, ok := iResp0.(*image.ImageListOK)
		if ok {
			// the error response has a payload
			if !swag.IsZero(eresp0) && !swag.IsZero(eresp0.Payload) {
				msgStr, err := json.Marshal(eresp0.Payload)
				if err != nil {
					return "", err
				}
				return string(msgStr), nil
			}
		}
		var iResp1 any = respErr
		eresp1, ok := iResp1.(*image.ImageListInternalServerError)
		if ok {
			// the error response has a payload
			if !swag.IsZero(eresp1) && !swag.IsZero(eresp1.Payload) {
				msgStr, err := json.Marshal(eresp1.Payload)
				if err != nil {
					return "", err
				}
				return string(msgStr), nil
			}
		}
		return "", respErr
	}

	// success responses
	if !swag.IsZero(resp0) && !swag.IsZero(resp0.Payload) {
		msgStr, err := json.Marshal(resp0.Payload)
		if err != nil {
			return "", err
		}
		return string(msgStr), nil
	}
	return "", nil
}
