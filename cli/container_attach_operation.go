// Code generated by go-swagger; DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"

	"github.com/go-swagger/dockerctl/client/container"

	"github.com/spf13/cobra"

	"github.com/go-openapi/swag"
)

// makeOperationContainerContainerAttachCmd returns a command to handle operation containerAttach
func makeOperationContainerContainerAttachCmd() (*cobra.Command, error) {
	cmd := &cobra.Command{
		Use: "ContainerAttach",
		Short: `Attach to a container to read its output or send it input. You can attach to the same container multiple times and you can reattach to containers that have been detached.

Either the ` + "`" + `stream` + "`" + ` or ` + "`" + `logs` + "`" + ` parameter must be ` + "`" + `true` + "`" + ` for this endpoint to do anything.

See [the documentation for the ` + "`" + `docker attach` + "`" + ` command](https://docs.docker.com/engine/reference/commandline/attach/) for more details.

### Hijacking

This endpoint hijacks the HTTP connection to transport ` + "`" + `stdin` + "`" + `, ` + "`" + `stdout` + "`" + `, and ` + "`" + `stderr` + "`" + ` on the same socket.

This is the response from the daemon for an attach request:

` + "`" + `` + "`" + `` + "`" + `
HTTP/1.1 200 OK
Content-Type: application/vnd.docker.raw-stream

[STREAM]
` + "`" + `` + "`" + `` + "`" + `

After the headers and two new lines, the TCP connection can now be used for raw, bidirectional communication between the client and server.

To hint potential proxies about connection hijacking, the Docker client can also optionally send connection upgrade headers.

For example, the client sends this request to upgrade the connection:

` + "`" + `` + "`" + `` + "`" + `
POST /containers/16253994b7c4/attach?stream=1&stdout=1 HTTP/1.1
Upgrade: tcp
Connection: Upgrade
` + "`" + `` + "`" + `` + "`" + `

The Docker daemon will respond with a ` + "`" + `101 UPGRADED` + "`" + ` response, and will similarly follow with the raw stream:

` + "`" + `` + "`" + `` + "`" + `
HTTP/1.1 101 UPGRADED
Content-Type: application/vnd.docker.raw-stream
Connection: Upgrade
Upgrade: tcp

[STREAM]
` + "`" + `` + "`" + `` + "`" + `

### Stream format

When the TTY setting is disabled in [` + "`" + `POST /containers/create` + "`" + `](#operation/ContainerCreate), the stream over the hijacked connected is multiplexed to separate out ` + "`" + `stdout` + "`" + ` and ` + "`" + `stderr` + "`" + `. The stream consists of a series of frames, each containing a header and a payload.

The header contains the information which the stream writes (` + "`" + `stdout` + "`" + ` or ` + "`" + `stderr` + "`" + `). It also contains the size of the associated frame encoded in the last four bytes (` + "`" + `uint32` + "`" + `).

It is encoded on the first eight bytes like this:

` + "`" + `` + "`" + `` + "`" + `go
header := [8]byte{STREAM_TYPE, 0, 0, 0, SIZE1, SIZE2, SIZE3, SIZE4}
` + "`" + `` + "`" + `` + "`" + `

` + "`" + `STREAM_TYPE` + "`" + ` can be:

- 0: ` + "`" + `stdin` + "`" + ` (is written on ` + "`" + `stdout` + "`" + `)
- 1: ` + "`" + `stdout` + "`" + `
- 2: ` + "`" + `stderr` + "`" + `

` + "`" + `SIZE1, SIZE2, SIZE3, SIZE4` + "`" + ` are the four bytes of the ` + "`" + `uint32` + "`" + ` size encoded as big endian.

Following the header is the payload, which is the specified number of bytes of ` + "`" + `STREAM_TYPE` + "`" + `.

The simplest way to implement this protocol is the following:

1. Read 8 bytes.
2. Choose ` + "`" + `stdout` + "`" + ` or ` + "`" + `stderr` + "`" + ` depending on the first byte.
3. Extract the frame size from the last four bytes.
4. Read the extracted size and output it on the correct output.
5. Goto 1.

### Stream format when using a TTY

When the TTY setting is enabled in [` + "`" + `POST /containers/create` + "`" + `](#operation/ContainerCreate), the stream is not multiplexed. The data exchanged over the hijacked connection is simply the raw data from the process PTY and client's ` + "`" + `stdin` + "`" + `.
`,
		RunE: runOperationContainerContainerAttach,
	}

	if err := registerOperationContainerContainerAttachParamFlags(cmd); err != nil {
		return nil, err
	}

	return cmd, nil
}

// runOperationContainerContainerAttach uses cmd flags to call endpoint api
func runOperationContainerContainerAttach(cmd *cobra.Command, args []string) error {
	appCli, err := makeClient(cmd, args)
	if err != nil {
		return err
	}
	// retrieve flag values from cmd and fill params
	params := container.NewContainerAttachParams()
	if err, _ = retrieveOperationContainerContainerAttachDetachKeysFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachIDFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachLogsFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachStderrFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachStdinFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachStdoutFlag(params, "", cmd); err != nil {
		return err
	}
	if err, _ = retrieveOperationContainerContainerAttachStreamFlag(params, "", cmd); err != nil {
		return err
	}
	if dryRun {
		logDebugf("dry-run flag specified. Skip sending request.")
		return nil
	}
	// make request and then print result
	msgStr, err := parseOperationContainerContainerAttachResult(appCli.Container.ContainerAttach(params))
	if err != nil {
		return err
	}

	if !debug {
		fmt.Println(msgStr)
	}

	return nil
}

// registerOperationContainerContainerAttachParamFlags registers all flags needed to fill params
func registerOperationContainerContainerAttachParamFlags(cmd *cobra.Command) error {
	if err := registerOperationContainerContainerAttachDetachKeysParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachIDParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachLogsParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachStderrParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachStdinParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachStdoutParamFlags("", cmd); err != nil {
		return err
	}
	if err := registerOperationContainerContainerAttachStreamParamFlags("", cmd); err != nil {
		return err
	}
	return nil
}

func registerOperationContainerContainerAttachDetachKeysParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagDetachKeysDescription := `Override the key sequence for detaching a container.Format is a single character ` + "`" + `[a-Z]` + "`" + ` or ` + "`" + `ctrl-<value>` + "`" + ` where ` + "`" + `<value>` + "`" + ` is one of: ` + "`" + `a-z` + "`" + `, ` + "`" + `@` + "`" + `, ` + "`" + `^` + "`" + `, ` + "`" + `[` + "`" + `, ` + "`" + `,` + "`" + ` or ` + "`" + `_` + "`" + `.`

	var flagDetachKeysName string
	if cmdPrefix == "" {
		flagDetachKeysName = "detachKeys"
	} else {
		flagDetachKeysName = fmt.Sprintf("%v.detachKeys", cmdPrefix)
	}

	var flagDetachKeysDefault string

	_ = cmd.PersistentFlags().String(flagDetachKeysName, flagDetachKeysDefault, flagDetachKeysDescription)

	return nil
}

func registerOperationContainerContainerAttachIDParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagIDDescription := `Required. ID or name of the container`

	var flagIDName string
	if cmdPrefix == "" {
		flagIDName = "id"
	} else {
		flagIDName = fmt.Sprintf("%v.id", cmdPrefix)
	}

	var flagIDDefault string

	_ = cmd.PersistentFlags().String(flagIDName, flagIDDefault, flagIDDescription)

	return nil
}

func registerOperationContainerContainerAttachLogsParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagLogsDescription := `Replay previous logs from the container.

This is useful for attaching to a container that has started and you want to output everything since the container started.

If ` + "`" + `stream` + "`" + ` is also enabled, once all the previous output has been returned, it will seamlessly transition into streaming current output.
`

	var flagLogsName string
	if cmdPrefix == "" {
		flagLogsName = "logs"
	} else {
		flagLogsName = fmt.Sprintf("%v.logs", cmdPrefix)
	}

	var flagLogsDefault bool

	_ = cmd.PersistentFlags().Bool(flagLogsName, flagLogsDefault, flagLogsDescription)

	return nil
}

func registerOperationContainerContainerAttachStderrParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagStderrDescription := `Attach to ` + "`" + `stderr` + "`" + ``

	var flagStderrName string
	if cmdPrefix == "" {
		flagStderrName = "stderr"
	} else {
		flagStderrName = fmt.Sprintf("%v.stderr", cmdPrefix)
	}

	var flagStderrDefault bool

	_ = cmd.PersistentFlags().Bool(flagStderrName, flagStderrDefault, flagStderrDescription)

	return nil
}

func registerOperationContainerContainerAttachStdinParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagStdinDescription := `Attach to ` + "`" + `stdin` + "`" + ``

	var flagStdinName string
	if cmdPrefix == "" {
		flagStdinName = "stdin"
	} else {
		flagStdinName = fmt.Sprintf("%v.stdin", cmdPrefix)
	}

	var flagStdinDefault bool

	_ = cmd.PersistentFlags().Bool(flagStdinName, flagStdinDefault, flagStdinDescription)

	return nil
}

func registerOperationContainerContainerAttachStdoutParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagStdoutDescription := `Attach to ` + "`" + `stdout` + "`" + ``

	var flagStdoutName string
	if cmdPrefix == "" {
		flagStdoutName = "stdout"
	} else {
		flagStdoutName = fmt.Sprintf("%v.stdout", cmdPrefix)
	}

	var flagStdoutDefault bool

	_ = cmd.PersistentFlags().Bool(flagStdoutName, flagStdoutDefault, flagStdoutDescription)

	return nil
}

func registerOperationContainerContainerAttachStreamParamFlags(cmdPrefix string, cmd *cobra.Command) error {

	flagStreamDescription := `Stream attached streams from the time the request was made onwards`

	var flagStreamName string
	if cmdPrefix == "" {
		flagStreamName = "stream"
	} else {
		flagStreamName = fmt.Sprintf("%v.stream", cmdPrefix)
	}

	var flagStreamDefault bool

	_ = cmd.PersistentFlags().Bool(flagStreamName, flagStreamDefault, flagStreamDescription)

	return nil
}

func retrieveOperationContainerContainerAttachDetachKeysFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("detachKeys") {

		var flagDetachKeysName string
		if cmdPrefix == "" {
			flagDetachKeysName = "detachKeys"
		} else {
			flagDetachKeysName = fmt.Sprintf("%v.detachKeys", cmdPrefix)
		}

		flagDetachKeysValue, err := cmd.Flags().GetString(flagDetachKeysName)
		if err != nil {
			return err, false
		}
		m.DetachKeys = &flagDetachKeysValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachIDFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("id") {

		var flagIDName string
		if cmdPrefix == "" {
			flagIDName = "id"
		} else {
			flagIDName = fmt.Sprintf("%v.id", cmdPrefix)
		}

		flagIDValue, err := cmd.Flags().GetString(flagIDName)
		if err != nil {
			return err, false
		}
		m.ID = flagIDValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachLogsFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("logs") {

		var flagLogsName string
		if cmdPrefix == "" {
			flagLogsName = "logs"
		} else {
			flagLogsName = fmt.Sprintf("%v.logs", cmdPrefix)
		}

		flagLogsValue, err := cmd.Flags().GetBool(flagLogsName)
		if err != nil {
			return err, false
		}
		m.Logs = &flagLogsValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachStderrFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("stderr") {

		var flagStderrName string
		if cmdPrefix == "" {
			flagStderrName = "stderr"
		} else {
			flagStderrName = fmt.Sprintf("%v.stderr", cmdPrefix)
		}

		flagStderrValue, err := cmd.Flags().GetBool(flagStderrName)
		if err != nil {
			return err, false
		}
		m.Stderr = &flagStderrValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachStdinFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("stdin") {

		var flagStdinName string
		if cmdPrefix == "" {
			flagStdinName = "stdin"
		} else {
			flagStdinName = fmt.Sprintf("%v.stdin", cmdPrefix)
		}

		flagStdinValue, err := cmd.Flags().GetBool(flagStdinName)
		if err != nil {
			return err, false
		}
		m.Stdin = &flagStdinValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachStdoutFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("stdout") {

		var flagStdoutName string
		if cmdPrefix == "" {
			flagStdoutName = "stdout"
		} else {
			flagStdoutName = fmt.Sprintf("%v.stdout", cmdPrefix)
		}

		flagStdoutValue, err := cmd.Flags().GetBool(flagStdoutName)
		if err != nil {
			return err, false
		}
		m.Stdout = &flagStdoutValue

	}

	return nil, retAdded
}

func retrieveOperationContainerContainerAttachStreamFlag(m *container.ContainerAttachParams, cmdPrefix string, cmd *cobra.Command) (error, bool) {
	retAdded := false
	if cmd.Flags().Changed("stream") {

		var flagStreamName string
		if cmdPrefix == "" {
			flagStreamName = "stream"
		} else {
			flagStreamName = fmt.Sprintf("%v.stream", cmdPrefix)
		}

		flagStreamValue, err := cmd.Flags().GetBool(flagStreamName)
		if err != nil {
			return err, false
		}
		m.Stream = &flagStreamValue

	}

	return nil, retAdded
}

// parseOperationContainerContainerAttachResult parses request result and return the string content
func parseOperationContainerContainerAttachResult(resp0 *container.ContainerAttachOK, respErr error) (string, error) {
	if respErr != nil {

		// responses
		// Non schema case: warning containerAttachSwitchingProtocols is not supported
		// Non schema case: warning containerAttachOK is not supported
		var iResp2 any = respErr
		eresp2, ok := iResp2.(*container.ContainerAttachBadRequest)
		if ok {
			// the error response has a payload
			if !swag.IsZero(eresp2) && !swag.IsZero(eresp2.Payload) {
				msgStr, err := json.Marshal(eresp2.Payload)
				if err != nil {
					return "", err
				}
				return string(msgStr), nil
			}
		}
		var iResp3 any = respErr
		eresp3, ok := iResp3.(*container.ContainerAttachNotFound)
		if ok {
			// the error response has a payload
			if !swag.IsZero(eresp3) && !swag.IsZero(eresp3.Payload) {
				msgStr, err := json.Marshal(eresp3.Payload)
				if err != nil {
					return "", err
				}
				return string(msgStr), nil
			}
		}
		var iResp4 any = respErr
		eresp4, ok := iResp4.(*container.ContainerAttachInternalServerError)
		if ok {
			// the error response has a payload
			if !swag.IsZero(eresp4) && !swag.IsZero(eresp4.Payload) {
				msgStr, err := json.Marshal(eresp4.Payload)
				if err != nil {
					return "", err
				}
				return string(msgStr), nil
			}
		}
		return "", respErr
	}

	// success responses
	// warning: non schema response containerAttachOK is not supported by go-swagger cli yet.
	return "", nil
}
