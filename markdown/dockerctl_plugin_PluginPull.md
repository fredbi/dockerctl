## dockerctl plugin PluginPull

Pulls and installs a plugin. After the plugin is installed, it can be enabled using the [`POST /plugins/{name}/enable` endpoint](#operation/PostPluginsEnable).


```
dockerctl plugin PluginPull [flags]
```

### Options

```
      --X-Registry-Auth string   A base64-encoded auth configuration to use when pulling a plugin from a registry. [See the authentication section for details.](#section/Authentication)
  -h, --help                     help for PluginPull
      --name :latest             Local name for the pulled plugin.
                                 
                                 The :latest tag is optional, and is used as the default if omitted.
                                 
      --remote :latest           Required. Remote reference for plugin to install.
                                 
                                 The :latest tag is optional, and is used as the default if omitted.
                                 
```

### Options inherited from parent commands

```
      --base-path string   For example: /v1.41 (default "/v1.41")
      --config string      config file path
      --debug              output debug logs
      --dry-run            do not send the request to server
      --hostname string    hostname of the service (default "localhost")
      --scheme string      Choose from: [http https] (default "http")
```

### SEE ALSO

* [dockerctl plugin](dockerctl_plugin.md)	 - 

###### Auto generated by spf13/cobra on 7-Nov-2025
