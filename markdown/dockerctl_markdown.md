## dockerctl markdown

Generate markdown documentation (that you might be reading right now!!)

### Synopsis

Generate markdown documentation (that you might be reading right now!!)

```
dockerctl markdown
```

### Options

```
      --dir string   The destination directory to save docs in (default "./markdown/")
  -h, --help         help for markdown
```

### Options inherited from parent commands

```
      --base-path string   For example: /v1.41 (default "/v1.41")
      --config string      config file path
      --debug              output debug logs
      --dry-run            do not send the request to server
      --hostname string    hostname of the service (default "localhost")
      --scheme string      Choose from: [http https] (default "http")
```

### SEE ALSO

* [dockerctl](dockerctl.md)	 - 

